export interface SinglePointOfInterest {
    lat: number;
    lon: number;
    title: string;
    text: string;
    imgCount: string;
}

export interface Coordinates {
    lat: number;
    lng: number;
}
